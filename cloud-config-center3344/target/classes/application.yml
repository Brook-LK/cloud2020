server:
  port: 3344

spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          #uri: https://github.com/Brook-LK/springcloud-config.git
          uri: D:/IdeaProjects/cloud2020/springcloud-config
          search-paths:
            - springcloud-config
      label: master

eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/